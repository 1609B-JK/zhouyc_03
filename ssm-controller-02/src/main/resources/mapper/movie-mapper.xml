<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >  
<mapper namespace="com.jk.mapper.movie.MovieMapper">


    <select id="selectBannerList" resultType="movieResponse" >
        SELECT * FROM
          (SELECT m.* , rownum r FROM
                (SELECT
                  t_id as "movieID",
                  t_title as "movieTitle",
                  t_content as "movieContent",
                  t_imgpath as "movieImgPath",
                  t_linkurl as "movieLinkUrl"
            FROM t_movie_zy) m
          WHERE rownum <![CDATA[<=]]>6) m1
        WHERE r >=1

    </select>

</mapper>   